# Runtime
pytest
pytest-cov
pydantic
pandas
numpy
scipy
pyyaml
python-dotenv
openai
huggingface_hub[cli]>=0.24.0
# Pinned <5.0: transformers 5.x's stricter "heterogeneous per-layer config"
# access (AmbiguousGlobalPerLayerAttributeError) is incompatible with
# vllm==0.19.0's model_arch_config_convertor, which reads config.head_dim
# directly. Keep this pin until vllm is upgraded past 0.19.0.
transformers==4.57.6
vllm==0.19.0; platform_system == "Linux"
scikit-learn
matplotlib
seaborn
statsmodels>=0.14.6
langdetect
streamlit

# Dev / formatting
black
ruff
